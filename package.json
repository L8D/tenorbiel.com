{
  "name": "tenorbiel",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build:2023": "cd 2023 && npm install && npm run build",
    "build:2026": "cd 2026 && npm install && npm run build",
    "build": "npm run build:2023 && npm run build:2026 && node assemble.js"
  }
}
